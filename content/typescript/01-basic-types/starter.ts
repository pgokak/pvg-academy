// Exercise: Add type annotations to everything below.
// The logic is already correct — your job is to add types.

// 1. Annotate these variables
const username = "Prashant";
const score = 100;
const isLoggedIn = true;

// 2. Annotate this function's parameter and return type
function double(n) {
  return n * 2;
}

// 3. Annotate this array
const languages = ["TypeScript", "JavaScript", "Java"];

// 4. Fix this function — it should only accept strings, not numbers
function greet(name) {
  return "Hello, " + name;
}
